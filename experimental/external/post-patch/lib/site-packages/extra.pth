import sys, os; pythonpath = os.environ['PYTHONPATH'] if 'PYTHONPATH' in os.environ else ""; [sys.path.append(p) for p in pythonpath.split(';')];
import sys, os; sys.path.append(os.path.dirname(os.path.realpath(sys.argv[0])));
import os; [os.add_dll_directory(p) for p in os.environ.get('PATH').split(os.pathsep)];
